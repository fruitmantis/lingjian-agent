# AGENTS.md

## 当前项目与唯一开发基线

- 产品：**伴飞 / 伴飞 Agent**，面向公司内部人员的伙伴智能助手，不开放外部伙伴账号。
- 正式工作目录：`/home/yuan/project/lingjian-agent-enablement`；当前开发分支：`main`。
- 该目录已升格为正式 main 工作区，不再是独立功能试验环境。legacy 目录、旧 feature 分支和其他历史 worktree 均不得作为正式开发环境；不修改或启动 legacy 服务。
- WSL Ubuntu 24.04；前端 Next.js 15 / React 19 / TypeScript，端口 **3000**；后端 FastAPI / Python，端口 **8000**。
- 正式数据库：**PostgreSQL 16，schema version 12**；本机数据库 `banfei_agent`，应用用户 `banfei_app`，SQLAlchemy Core / psycopg。当前无 Alembic。
- 连接由私有 `.isolation/runtime/dev/environment.json` 中 `DATABASE_URL` 指定，缺失或失败直接报错，不自动回退其他数据库。
- 上传目录 `.isolation/runtime/dev/uploads`，日志 `.isolation/logs/`；原 `.isolation/runtime/dev/app.db` 及迁移备份只作保留，不参与日常运行。
- 工程、模块、API、字段及 `LINGJIAN_*` 技术标识不因产品更名而重命名。
- 实际代码、配置和本次核验优先于历史聊天与归档文档。首次操作先检查工作目录、分支、Git 状态和服务归属，不输出凭据。

## 产品入口与核心流程

- 左侧：开启新任务、场景广场、伙伴洞察、资源中心、全部任务与历史任务、个人中心；管理员另有管理后台。
- `/` 提供显式“项目找伙伴 / 能力发展”两个 Tab；`/?mode=development` 直接进入能力发展。不自动识别两条业务模式。
- 项目找伙伴：自然语言需求 → 读取现有伙伴标签、标准行业/区域、画像摘要、案例摘要和交付物名称 → 推荐伙伴、分数、理由、支撑证据与风险 → 需求画像和项目机会。资料不足自动降级，不新增人工整理流程。
- 能力发展：**伙伴 + 自然语言方向** → 顾问式建议与资源 → 继续问伴飞。解释/讨论/比较不创建 Version，修改成功产生新 Version；草稿不需确认即可正常使用。
- 伙伴详情、匹配结果及共享案例的发展入口汇入统一新任务页，保留允许的来源上下文。原匹配风险不直接当作已确认能力短板。
- 两类任务共用 `/tasks` 和 `/tasks/{id}`。每份 Plan 只有一个主入口，Run 历史在内部；采用版本、历史版本、运行记录保持二级操作。
- 不在普通用户界面提供结构化高级编辑入口；已有后台编辑 API 和历史版本兼容能力保留。
- 资源中心独立浏览课程、实验和已发布共享案例，不要求先选伙伴或手工选资源。只记录“发起跳转”，不宣称学习完成或能力提升。
- `/enablement` 只用于兼容跳转，不提供另一套工作台。场景广场是已有功能的快捷入口，不是新的 Agent 平台。
- 管理后台：任务、伙伴/资料/案例共享、课程实验及发布核验、需求画像、项目机会、运营统计、标签、用户、模型和系统状态。
- 项目机会抽取允许常见结构差异，缺失字段标记“未知”；未知不计完整度。网络、认证、事务失败仍真实报错，不掩盖技术失败。

## 必须保留的业务与安全约束

- 只有 `user` / `admin` 两角色；管理接口在后端校验 admin，不以 UI 隐藏代替授权。
- 公开业务 API 仅 `GET /health`、`POST /auth/login`、`POST /auth/user-applications`；其他业务接口要求有效 Token。
- 匹配归属 `match_records.owner_user_id`，发展方案归属 `development_plans.owner_user_id`。列表、详情、编辑、重试、归档/恢复及关联业务必须校验所有权，普通用户越权访问返回 404。
- 普通用户只读访问启用伙伴及允许资料；伙伴、附件、案例、交付物与画像写操作仅限管理员。
- 启用/停用为伙伴正常生命周期。管理员硬删除只允许无业务历史的伙伴；有案例、资料、匹配历史或发展方案等关联时拒绝并返回计数；派生数据可安全清理。不得删除用户或添加回收站等新生命周期。
- 系统可见、允许模型发送、允许伙伴外发三维权限互不推导。共享案例只使用当前获授权的共享版本，不以内部案例原文代替。
- 伙伴可传递视图只取当前 confirmed 指针，输出时重检权限、版本和撤权状态，应用字段白名单；不得泄漏内部证据、备注或模型内部字段。
- 保留 Plan / Run / Version、current / confirmed、submission 幂等、并发串行化、版本冲突、事务、超时和中断恢复。失败 Run 不覆盖既有有效版本，也不把仍可用方案表达为整体失败。
- 行业/区域唯一标准是 `shared/business-taxonomy.json`：12 个行业；国内省级、海外六区域，支持两组同时多选。无法确认的旧分类保留待确认，不猜测、不改写自由文本。
- 正式能力标签由管理员维护；AI 建议采纳后才成为正式标签。资料缺失不等于缺乏能力，画像不等于个人能力评估。
- 伙伴健康度是临时实现，正式指标等待外部平台，不自行增加评分算法。

## 账号安全

- 登录页默认仅登录；“注册”进入内部申请，含姓名、工号、部门、邮箱、用户名、密码、确认密码及可选说明。管理员审批后首次登录必须改密。
- 密码只存不可逆哈希；申请批准/驳回后清除申请密码哈希；管理员创建的临时密码只展示一次。
- 保留停用/角色变更/改密/重置/注销全部会话后旧 Token 即时失效；连续 5 次失败锁定 15 分钟、管理员解锁、自停用/自降级和最后有效管理员保护。
- 无固定默认账号密码；`JWT_SECRET_KEY` 必须显式配置且至少 32 位。已有数据库不重新执行 bootstrap。

## 模型与测试桩

- 当前七个场景及默认模型已显式绑定 `api.deepseek.com` / `deepseek-v4-flash`。不擅自修改地址、Key、模型、默认配置或场景绑定。
- 用户已授权按现有业务权限发送所需伙伴资料/画像、案例说明、项目需求、对话和授权资源；伙伴画像生成包括上传文档提取文本。能力发展仍按现有最小上下文规则排除内部附件及案例原文。
- 日常开发和人工体验用真实模型；本地 mock 服务停用。历史 mock 配置因 Run 引用保留，不能随意删除或重新设为默认。
- 单元故障注入和显式 replay 只在隔离测试环境使用。模型集成测试须限定场景、数据和次数，记录实际请求；批量业务调用需事先说明范围/费用，不自动批量执行。
- 普通 UI、文档、静态检查不调用模型。不向用户展示 Prompt、原始模型 JSON、密钥、内部日志或异常堆栈。
- 结构容错不等于放宽资源 ID/版本、URL、权限、enum 和强结论来源校验；能力发展不回退到“首个启用模型”。

## 视觉与范围

- 保持黑白灰与华为红 `#C7000B`，红色原图标 + “伴飞 Agent”字标；保留成功、警告、错误语义色。
- 目录卡片、输入容器、表格、控件遵循现有共享样式，不重新设计导航或 Logo。官方 Latin/数字字体本地加载；中文沿用已批准的系统 fallback，不假称中文 Web Font 全覆盖。
- 当前未接入 Chroma、Embedding、向量检索或 RAG 基础设施；资源主题出现 RAG 不代表系统使用 RAG。
- 未经明确要求不引入 Redis、队列、微服务、复杂 RBAC、LMS、Planner、Multi-Agent、新数据库迁移框架或其他架构扩展。

## Git、数据与执行约束

- 单人直接在 `main` 迭代，不新建/切换分支或 worktree，不擅自合并、rebase、reset、revert、clean 或丢弃已有改动。
- 只有用户明确要求时才 commit、push 或部署；授权不互相推导，禁止 force push。远端冲突或分支保护阻止时报告，不绕过。
- 只读分析不修改文件；实施采用最小范围，UI/文案不顺带改业务。保留历史资料但不把归档命令当运行规范。
- 不删除、清空、重建、重新 seed 或替换现有数据库，不清理上传、私有快照、忽略文件、legacy 目录或备份。必要数据修改须明确范围、事务化；结构变化先说明影响、备份及回退。
- 不提交数据库、真实业务材料、签审原件、环境配置、密钥或敏感日志。
- 自动化测试仅用本机 `banfei_validation` 临时 schema；SQLite 兼容测试及上传文件只用 `/tmp`。不得对 `banfei_agent` 或 ARM 运行库执行测试 seed、故障注入或 E2E 写操作。

## 服务与验证

- 在正式工作目录使用 `bash enablement-dev.sh status|start|stop`，沿用已有私有配置；不得绕开配置重建环境。停止前核对 PID/端口与项目归属，禁用宽泛 kill。
- 开发服务、production build、Playwright 不得并行写同一个 `.next`。使用独立构建目录，或验证归属后停服再恢复。
- PostgreSQL 验证入口：`.venv/bin/python scripts/run_postgres_validation.py backend|browser`，由私有 `BANFEI_TEST_DATABASE_URL` 指向本机专用验证库。
- 默认 Playwright 只跑选定 UI 子集；完整回放需显式 `PLAYWRIGHT_MODEL_MODE=replay`，与真实供应商验证分开。相关命令见 README，不把 SQLite 测试冒充 PostgreSQL 验证。
- 按本次影响范围验证，不无故扩大测试。结束后报告修改范围、接口/数据影响、实际测试、未确认事项和 Git 状态；工程通过不代表业务上线许可。
