# 伙伴服务能力发展中心：Phase 0 + Phase A 交付报告

记录日期：2026-09-06。范围止于 Phase A；没有进入 Phase B/C，没有合并、push、部署或替换旧服务。

已完整读取 V1.1 正文及表格，需求定义取第27章，验收取第29章；用户本轮四项实施口径优先于文档较宽松表述。原DOCX SHA256：`4ab64c866d7d08893b6206ef14adc7a1a3ddcab5edac39b87dda9863ed8f857a`。下列核验数量来自本轮查询，未把文档数字直接当成实查。

## 1. 旧版基线及保护检查

| 项目 | 核验结果 |
|---|---|
| 原目录 | `/home/yuan/project/lingjian-agent` |
| 分支 / commit | `main` / `f79cbb29ec7b3ad70c88e618e3161211f211c06a` |
| 唯一本地标签 | `baseline/pre-enablement-20260905-f79cbb2` |
| 工作区 | 基线和收尾均干净；未stash、提交、覆盖原目录内容 |
| 后端 | PID 227786，原目录启动，8000，19:19:14启动 |
| 前端 | PID 236033，原frontend启动，3000，20:33:59启动 |
| 运行对应 | 源码/配置修改时间均早于对应进程启动，无路径覆盖或源码差异 |
| 文件保护 | 163个受保护文件摘要无变化，覆盖原跟踪文件、配置和上传 |
| 原数据库 | 所有原表逐行摘要与一致性快照相同，schema仍为9 |
| 服务保护 | 全程未停止旧版，相同PID保留，健康检查/前端HTTP200 |
| Git保护 | main指针不动；原视觉/备份分支保留；只新增授权标签、分支/worktree和本地提交 |

运行对应证据是源码、配置、工作目录和进程启动时间，未做进程内存逐字节比对。本轮未核验GitHub服务端，不声称远端同步；未执行任何远端写入。

## 2. 新分支、worktree与独立环境

- 分支：`feature/partner-enablement-v1.1`。
- worktree：`/home/yuan/project/lingjian-agent-enablement`。
- 一致性快照：`.isolation/snapshots/baseline.db`。旧库以只读连接打开，通过SQLite `Connection.backup`获取，再通过backup创建独立运行库；未复制活动单个db冒充备份。
- 新运行库：`.isolation/runtime/app.db`；新上传：`.isolation/runtime/uploads`；Chroma只保留独立空目录，未接入。
- 8个上传文件为独立普通副本，inode不同；副本中存储路径重定位到新上传目录，没有旧目录数据软链接/硬链接。此路径变换发生在业务迁移之前，单独核对。
- 新 `.env`、独立JWT、前端 `.env.local`、CORS均指向3100/8100。浏览器localStorage按origin隔离；旧Token不能直接用于新JWT环境。
- 独立 `.venv`、node_modules、`.next`、缓存及日志；系统Python/Node可执行文件共用，依赖目录不共用。
- 实际端口：前端3100、后端8100；E2E mock18180。pytest使用临时库及动态故障测试端口，E2E使用 `/tmp/lingjian-enablement-e2e/app.db`；未在运行副本创建测试账号或录入合成资源。
- `.isolation`为私有目录，数据/配置/日志均忽略。新环境保留模型禁用配置；专用运行器另在连接层阻断非回环出站，已实际验证阻断与回环连接可用。请勿绕过专用运行器启动快照环境。

实际环境：Python3.12.3、Node18.19.1、npm9.2.0、Next15.5.19、React19.2.7、Playwright1.49.1；后端依赖版本保存于私有 `environment.json`。

## 3. Phase A 已实现及需求对应

| 内容 | 需求编号 | 边界 |
|---|---|---|
| 课程/实验新增、编辑、发布、下架、结构化元数据 | RES-01、RES-03 | 管理后台完成，前台中心留B |
| 岗位、产品技术、难度、语言/站点、先修、时长、费用/账号/环境及来源 | RES-03 | 未知明确保留，未实施推荐约束匹配 |
| 复用正式能力标签及映射、发布前有效性校验 | RES-05 | 不增加第二套能力字典 |
| 人工核验人/服务端时间/链接状态/内容与授权依据，绑定修订 | RES-07 | 测试中的核验是合成数据，不代表真实链接已验 |
| 有效case_id/伙伴校验、独立共享草稿及不可变发布快照 | CASE-01、CASE-02 | 孤儿拒绝共享，内部案例更新不自动发布 |
| 贡献伙伴实际角色、案例归属/画像不因共享改变 | CASE-03、CASE-04 | 人工填角色，不自动归因能力 |
| 原user/admin和认证复用，14个新API操作全部require_admin | SEC-01、SEC-03 | 无外部登录或第二权限系统 |
| 三维用途默认拒绝，当前权限与发布快照权限共同校验 | SEC-04、SEC-06、SEC-07基础 | 资源层投影完成，方案级视图留C |
| 下架、敏感撤权、授权代次，重新勾选不复活旧内容 | CASE-07、SEC-09基础 | 方案历史说明文字撤权留C |
| source_type/id/version、后端解析URL、模型/外发字段白名单 | RES-04、MOD-02基础 | 服务契约完成，未实施模型生成链 |
| 固定错误提示、凭据与数据提交检查 | SEC-08 | 没有新增原始模型输出展示 |
| 增量迁移、权限/故障注入和原功能回归 | NFR-08及可靠性基础 | 不替代后续Plan/Run性能验收 |

新增页面 `/admin/resources`；原伙伴案例中“共享设置”进入 `/admin/partners/{id}/cases/{caseId}/sharing`。复用原App Shell、认证、蓝/青绿配色，未改普通用户导航或增加发展中心前台。

新增API提供资源列表/创建、详情/编辑、权限、核验、发布和下架；案例共享提供详情/保存及相同四种操作。实测OpenAPI为71 paths / 86 operations，仅原3个公开操作保留。

有共享配置的源案例删除增加409保护，首次共享与删除串行，避免删除审计来源或抛出外键500。原任务所有权、业务模型配置、匹配规则和健康度算法保持；schema/API增加属于本轮范围。

## 4. 本地提交

| commit | 主要变化 |
|---|---|
| `f3cb38e` | Phase 0隔离、基线验证、详细设计与测试环境修正 |
| `0353368` | Phase A数据/管理员API/共享权限、v10迁移与自动化测试 |
| `3231de1` | 管理后台、共享入口、E2E/截图、来源链接校验补充 |

本报告另作为文档提交。提交后的完整清单（含报告自身hash）保存在 `.isolation/evidence/commits.json`，完整报告副本为 `.isolation/DELIVERY_REPORT.md`。每批均核对暂存清单、diff和当前真实密钥匹配；数据库、上传、环境密钥和原始日志未提交。

## 5. 实际测试命令与结果

各次运行有重叠，不将通过数量相加当成独立用例数。所有命令在新worktree执行；前端命令位于frontend目录。

| 命令/阶段 | 通过 | 失败/错误 | 跳过 |
|---|---:|---:|---:|
| `.venv/bin/pytest -q`，基线 | 222 | 0 | 0 |
| `npm run typecheck`，基线及最终 | 通过 | 0 | 0 |
| `npm run build`，基线及Phase A | 通过 | 0 | 0 |
| `npm run test:e2e`，基线两轮 | 各36 | 各1 | 0 |
| 定向重跑task-navigation的submission用例，修正后 | 1 | 0 | 0 |
| 后端初轮新增专项 | 47 | 1个故障注入测试错误 | 0 |
| 后端首次全量接入 | 243 | 25个历史v9夹具错误 | 0 |
| 后端修正后全量 | 269 | 0 | 0 |
| 并发保护后enablement/migration/files/openapi专项 | 56 | 0 | 0 |
| `.venv/bin/pytest -q --junitxml=.isolation/evidence/pytest-final.xml`，并发保护后全量 | 270 | 0 | 0 |
| 最后URL校验补充后enablement/migration/openapi专项 | 52 | 0 | 0 |
| `npm run test:e2e -- enablement-admin.spec.ts` | 6 | 0 | 0 |
| `npm run test:e2e`，最终完整回归 | 43 | 0 | 0 |
| `.venv/bin/python backend/scripts/verify_enablement_snapshot.py` | 通过 | 0 | 0 |
| `bash enablement-dev.sh start/stop/status`，实际两次生命周期 | 通过 | 0 | 0 |

最后52项专项包含2个新数字/十六进制本机地址用例，不将其虚报为已包含在270项全量运行中。最终类型检查再次通过。后端2个依赖弃用警告不影响结果。

安装/初期失败处理：npm ci因Next可选peer与原Playwright锁不一致失败，独立分支固定原1.49.1并使用明确legacy-peer-deps，随后安装/构建通过；未升级旧环境。E2E原stale阈值2s短于slow mock单阶段5s，仅测试阈值改为60s。迁移测试把第10处误当DDL，修正为最后schema写入。历史修复工具仍严格只接受v9，其测试恢复为完整合成v9结构，并额外验证拒绝v10，未放宽维护保护。

覆盖原匹配、任务归属/导航/重试、需求和机会、伙伴洞察、场景、申请审批/会话失效、用户和管理员权限、模型配置解析、系统只读状态。新增覆盖授权组合、默认拒绝、草稿/发布分离、核验过期、版本冲突、撤权再授权、非法ID/URL、并发发布/源删除、失败原子性与恢复。

## 6. 迁移前后与既有异常

| 项目 | 前 | 后 |
|---|---:|---:|
| schema | 9 | 10 |
| 原业务表 | 16张 | 逐行摘要全部一致 |
| 新表 | 0 | 7 |
| 伙伴/案例/交付物 | 33/3/0 | 33/3/0 |
| 任务/需求画像/项目机会 | 48/48/48 | 48/48/48 |
| 用户/正式能力标签 | 1/24 | 1/24 |
| integrity_check | ok | ok |
| 既有外键异常 | cases rowid3、4 → partners | 原2条保留 |
| 新增外键异常 | — | 0 |
| 重复迁移 | — | 完全一致 |

只在副本与测试库迁移。多个DDL节点及schema写入失败全部回滚，旧表保留；修正后可重试。发布指针故障不残留有效半版本。恢复测试使用backup生成独立新文件，不覆盖活动数据库或重建原库。副本上传路径调整在迁移前单独完成，不混淆为业务数据修改。

## 7. 证据位置

- [开发启动及完整最小详细设计](STARTUP_AND_DESIGN.md)
- [后端批次](PHASE_A_BACKEND.md)、[界面批次](PHASE_A_UI.md)
- [资源列表1366](../../artifacts/enablement/resource-list-1366.png)、[资源编辑1366](../../artifacts/enablement/resource-editor-1366.png)、[案例共享1366](../../artifacts/enablement/case-sharing-1366.png)
- 同目录包含各1920版本，共6张合成截图；自动检查两个分辨率无横向溢出，并人工查看编辑页截图。
- 私有 `.isolation/evidence/` 保存migration.json、protection-check.json、pytest-final.xml、manifest.json、environment.json。
- 私有 `.isolation/logs/` 保存baseline-*与phase-a-*，包含失败和最终通过记录；Playwright报告在frontend/playwright-report，未入Git。

## 8. 未完成与未验证项

- Phase B前台资源中心、搜索/跳转、上下文入口和统一任务类型未实施；Phase C发展方案生成、调整、编辑、确认与外发未实施。
- 详细设计已覆盖：只有当前confirmed版本能外发；复制再次鉴权；资源缺口/证据不足为业务成功；技术部分失败不产有效版本；同Plan串行与base版本检查；迟到结果不能覆盖；运行中归档409；说明文字也记录来源依赖，撤权时隐藏受影响段落，依赖不完整则拒绝整个伙伴视图。这些尚未作为方案链路端到端实现/验收。
- 真实模型调用0次，未验证；mock通过不能代表真实模型质量。模型配置和业务场景没有更改。
- DATA-01/02与业务验收未完成：试点方向、真实课程/实验、共享授权/脱敏、链接人工核验和固定目标样例待业务确认；截图中核验与授权为合成测试，不是业务签审。
- 不修复2条孤儿案例、不填补历史材料、不改模型默认标记或健康度。
- 2000+1000资源检索性能、3并发发展方案、180s/10min超时、60s中断识别与Plan事务验收留后续阶段，本轮不声称通过。
- 原v8迁移回放依赖本地隔离历史备份，该备份不入Git；在新机器运行需提供受控备份或等价合成fixture。本环境实际通过，新v10迁移测试已有合成fallback。
- 报告生成时曾有附带的新环境配置清理被自动审批拦截，未执行；取消该清理后仅生成报告，现有模型禁用配置及连接层隔离保留。

## 9. 停止新版与继续使用旧版

新版验证服务已停止，旧版继续使用 `http://localhost:3000`（后端8000），不需要切分支或重启。

新worktree可用 `bash enablement-dev.sh start|stop|status` 管理独立3100/8100；启动遇端口冲突拒绝启动，停止仅作用于验证了PID/cwd/启动标识的新进程组，并等待退出。请勿在快照环境绕过专用运行器直接启动uvicorn、复制回旧目录或运行旧dev.sh替换服务。

这些启动/停止操作本轮已经实际执行，以上是保留环境的操作说明，不是交给用户补做验收。

## 10. 阶段结论

| 层次 | 结论 |
|---|---|
| Phase 0保护与隔离 | 已完成并核验 |
| Phase A代码 | 已实现，本地分批提交 |
| 自动化 | 上述构建/类型/后端/浏览器验证通过，初始失败及修正留存 |
| 真实模型 | 未执行 |
| 真实业务/资源验收 | 未通过，不判真实试点GO |
| 进入Phase B的工程条件 | 具备：资源/共享/授权骨架可用，迁移可回放，回归通过，旧版保护成立 |

Phase B需用户另行启动。前台接入须继续执行当前资源授权/下架校验并增加搜索和跳转契约，不能直接向普通用户暴露管理员接口。业务数据签审可并行准备，但不得用软件通过替代真实试点验收。本轮到此停止。
