> **历史归档，不是当前开发规范。** 保留当时的需求、环境、测试和结论；旧品牌、分支、端口、数据库及授权状态均不代表当前 main。历史命令不可直接用于现有运行库。当前入口：[README](../../../../README.md)。

# RC 工程视觉复核

视口：1366×768、1920×1080；截图为全页 PNG，长图高度不等于视口高度。所有业务内容均来自隔离 `/tmp` 合成测试库，未拍摄真实敏感材料。

## 最终截图人工图像抽查

| 证据（artifacts/enablement-rc 下） | 观察 |
|---|---|
| rc-states/04-failed-revise-list-1366.png | 任务行与侧栏主状态为绿色“方案可用”，当前/已确认 V1 同时可见；“最近调整失败”单独红色，无页面横向溢出；详情/归档按钮可见 |
| rc-states/05-interrupted-revise-detail-sidebar-1920.png | 详情与侧栏均保留可用方案与已确认 V1；“最近调整中断”和 Run 历史同时可见，旧方案正文/资源缺口仍可读 |
| rc-states/03-v2-draft-list-1920.png | “当前草稿 V2 / 已确认 V1”明确并列；主状态未把未确认 V2 当作确认版本；撤权历史项显示内容受限 |
| rc-states/06-archived-detail-sidebar-1366.png | 详情和侧栏“当前查看”均为中性“已归档”；版本/Run 保留，恢复按钮可见，确认/调整/复制相关操作禁用 |

自动化完整覆盖六类状态、两种页面（详情含侧栏 / 列表）、两种视口，共 24 张专项状态图；另保留完整回归产生的中心、资源、上下文等截图。路径、尺寸、SHA-256 见 `artifacts/enablement-rc/screenshot-manifest.json`。

自动化断言包括页面无横向溢出、当前侧栏条目可滚动至视口内、状态标签中心不被其他元素覆盖，以及列表/侧栏/详情文案一致。较早方案确认和当前方案归档通过真实 API 操作触发，未借刷新整页掩盖旧状态。

App Shell、浅灰背景、白色卡片、蓝色操作按钮和原导航位置延续现有实现；方案状态信息增加后每条历史任务较高，任务区独立滚动仍保留。没有新增历史列表或培训系统布局。

## 限定结论

状态分离的工程图像与自动化检查通过。原匹配任务仍使用原有已完成/部分完成/失败标签；红色失败状态未覆盖已有可用确认版的 Plan。

本检查不代表业务负责人已签署真实业务验收。伙伴可传递视图继续使用原白名单，其真实数据可读性待业务方逐字段评审，详见 `RC_PRODUCT_REVIEW.md`。
