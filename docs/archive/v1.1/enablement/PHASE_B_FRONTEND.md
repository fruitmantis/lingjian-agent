> **历史归档，不是当前开发规范。** 保留当时的需求、环境、测试和结论；旧品牌、分支、端口、数据库及授权状态均不代表当前 main。历史命令不可直接用于现有运行库。当前入口：[README](../../../../README.md)。

# Phase B 前台融合验证批次

已实现 NAV-01/02、SCN-01、PRT-01、MAT-01、RES-02/03/08、CASE-05 的前台入口与真实数据读取。助手只提供来源资料与诉求 UI，不创建发展任务、调用模型或生成方案。资源使用当前发布版本；详情和来源定时/聚焦重查，跳转前后端再次校验。

验证：`npm run typecheck --prefix frontend` 通过；`npm run test:e2e --prefix frontend -- enablement-workspace.spec.ts` 最终 **8 passed / 0 failed / 0 skipped**，包括两种分辨率共 18 张截图。

验证中修复：筛选 select 的可访问名称；原测试错误地将侧栏匹配任务的“已完成”当作资源跳转状态，现仅断言资源主体；截图断言保留实际“我的任务”页标题，并用拥有任务的 user A 验证。首次 1 通过 / 7 失败，第二次 6 通过 / 1 失败 / 1 未运行；第三次全部通过，失败未伪装为通过。

截图为 /tmp 自动化合成数据，存放 artifacts/enablement-phase-b。截图只隐藏 Next 开发工具浮层，不隐藏业务内容。后续继续完整原功能回归与生产构建。

收尾复核补齐发布核验人显示，仅提供内部显示名，不输出账号标识或审计备注。完整回归 296 项后端 / 51 项浏览器通过；该补充后定向重跑 26 项后端 / 8 项浏览器均通过，截图重新生成。最终 TypeScript 与生产构建通过。资源性能基准最终 P95 42.8ms（进程内目录查询）；完整证据见 artifacts/enablement-phase-b/verification.json。
